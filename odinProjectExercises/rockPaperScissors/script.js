const rock = 'rock';
const paper = 'paper';
const scissors = 'scissors'

function game(choice) {
    if (choice == rock) {
        console.log()
    } else if (choice == paper) {
        console.log()
    } else {
        console.log()
    }
}