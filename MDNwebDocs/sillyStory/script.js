const randomize = document.querySelector('.randomize');
const customname = document.querySelector('.customname');
const para = 'It was 94 fahrenheit outside, so Bob went for a walk. When they got to :inserty:, they stared in horror for a few moments, then :insertz:. Bob saw the whole thing, but was not surprised — :insertx: weighs 300 pounds, and it was a hot day.'

function create() {
    (value.customname == true) {
        para.replace('Bob', ${customname});
    } else {
        return 0
    }
}